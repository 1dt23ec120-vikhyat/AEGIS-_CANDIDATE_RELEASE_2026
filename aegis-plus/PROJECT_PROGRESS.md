# AEGIS+ — Project Progress

Operational tracking artifact for development continuity. **Not** part of the
official Knowledge Base. Updated at the end of every Work Package.

Legend: ✅ complete · 🟡 in progress · ⬜ not started · ⛔ blocked

---

## Phase 3 — Core Development

### Milestone 1 — Platform Foundation & Walking Skeleton

#### M1a — Platform Foundation

| WP | Title | Status | Notes |
|----|-------|--------|-------|
| WP1 | Repository scaffold & developer tooling | ✅ | Approved. Full tree + Black/Ruff/Mypy/Pytest/Import Linter/pre-commit, all green. |
| WP2 | Configuration subsystem | ✅ | Modular `config/` package (schemas, loader, validation, paths, environments). |
| WP3 | Logging & audit foundation | ✅ | Loguru centralized logging, audit sink, stdlib bridge, secret redaction. |
| WP4 | Core primitives | ✅ | Entities, value objects, centralized exceptions, ports (ILogger/IRepository/IConfigurationProvider/IAIService); DIP contracts + Domain Purity enforced. |
| WP5 | Persistence foundation | ✅ | SQLAlchemy `Database`, generic repository over `IRepository`, Unit of Work, entity↔row mappers, Alembic (authoritative), audit persistence via DI. |

**M1a — Platform Foundation: COMPLETE.** Knowledge Base synchronization from the Documentation Impact Log (D1–D22) is now due before M1b.

#### M1b — Walking Skeleton

| WP | Title | Status | Notes |
|----|-------|--------|-------|
| WP6 | Application bootstrap & composition root | ✅ | DI container, lifecycle, embedded FastAPI backend (threaded), startup/shutdown, background-service manager, health checks. |
| WP7 | Application shell & UI framework | ✅ | PySide6 shell (splash, main window, sidebar, top bar, status bar), token-driven Light/Dark themes, reusable component library, routing framework, 8 polished pages, backend HTTP client. `ui ⇏ services` contract activated. |
| WP8 | Walking skeleton | ✅ | Root entry point; end-to-end UI↔FastAPI↔persistence↔DB; startup applies migrations, verifies DB, persists first audit event (`application.start`); walking-skeleton integration tests. |
| M2 | URL Analysis vertical | ✅ | First functional detection feature, end-to-end: `Url`/`UrlScan` domain + `IUrlAnalyzer`/`IAuditTrail` ports; FESD feature extractor + explainable `HeuristicUrlAnalyzer`; `UrlAnalysisService` (validate→analyze→persist→audit); `url_scans` table + migration; `POST /api/url/scan` + recent; live URL Scanner result panel + dashboard integration. A user can submit a URL and receive a complete, explainable, persisted, audited analysis. |
| M3 | Threat Intelligence & Auto-Protection | ✅ | `ThreatEntry` aggregate + `IThreatIntelligenceRepository`/`IThreatProtectionService` ports; `ThreatIntelligenceService` (auto-blacklist on malicious, blacklist-hit short-circuit, guard-open); `threat_entries` table + migration; `/api/threats` (check/guard-open/list/stats/fetch); Threat Intelligence page, blocking warning dialog (no "Open Anyway"), scanner blacklist state + guarded open, dashboard threat stats; audit events for blacklist/hit/open-blocked/viewed. Malicious URLs are remembered, blocked, and prevented from reopening. |
| M4·P1 | Advanced URL Intelligence Engine — spine | ✅ | **Demonstration** LightGBM analyzer behind `IUrlAnalyzer` + heuristic **fallback** (infra `LightGbmModelLoader`, DI-only swap, SHAP explanations; bundled model trained on synthetic data — validates ML infrastructure, not a production classifier); **hybrid** evidence engine (`combine_evidence` pure policy over ML + heuristic + domain + reputation + threat-intel); offline **domain intelligence** (homograph/IDN/mixed-script/suspicious-TLD/embedded-creds) → security hardening; `IReputationProvider` + graceful null; **XAI 2.0** report (category, evidence strength, per-source scores); config-driven weights/thresholds; `url_scans` intelligence columns + migration; scanner intelligence-sources panel. Production model replaces the artifact without architectural changes. |
| M5·P1 | Email Threat Intelligence Engine | ✅ | Full **email vertical** reusing the platform: `EmailMessage`/`EmailAddress`/`EmailAttachment` domain (stdlib parse) + `EmailScan` aggregate; `IEmailAnalyzer`/`IEmailEvidenceProvider` ports; **HybridEmailAnalyzer** over 5 offline providers (header, SPF/DKIM/DMARC authentication, sender/brand-impersonation, social-engineering language, attachment metadata) combined through the **shared `combine_evidence`**; **URL engine reused as one evidence source** (each embedded URL runs through `UrlAnalysisService`); multi-artifact **Threat Intelligence** (`ThreatEntry.artifact_type` URL/EMAIL, generic `record_report`); `email_scans` table + migration; `/api/email/scan` + recent; live Email Scanner page (verdict, category, evidence strength, intelligence sources, embedded-URL results, XAI); threat list shows artifact type. Email categories: phishing/BEC/credential-harvesting/malware-delivery/brand-impersonation/suspicious-sender/spam. |
| M5·P2 | Enterprise Email Investigation & Intelligence | ✅ | **Analyst investigation workspace**: sectioned SOC console (Executive Summary, Overview, Authentication, Sender Intelligence, Threat Intelligence, Embedded URLs, Attachments, Message Body, Timeline, Explainable AI, Analyst Notes) built from a reusable collapsible `Section` component; full parsed metadata (CC/BCC/Date/Message-ID/MIME/Content-Type/Priority) + plain/HTML/raw-MIME body views; **per-mechanism SPF/DKIM/DMARC** breakdown (status ✓/⚠/✗ + reason + security impact) via `AuthMechanism`/`AuthStatus`; sender intelligence with reply-to comparison, brand-impersonation status and **historical detections**; attachment investigation (hash, indicators, YARA/malware/sandbox placeholders) using domain-owned classification reused by the AI provider; **analyst workflow** persisted (`EmailInvestigation` status/priority/tags/notes + table + migration + API). Category accuracy fix: authentication evidence is now category-neutral so BEC / credential-harvesting / brand-impersonation categorize correctly. |
| M6·P1 | Incident Intelligence & Campaign Correlation | ✅ | **Incident** and **Campaign** first-class aggregates; extensible correlation domain (`ArtifactKind`/`ArtifactRef`/`CorrelationLink` + pure `correlate` and `subject_pattern` policies) covering shared sender / reply-to / domain / subject pattern / URL / URL hash / attachment hash / category — new observable types (files, IPs, processes, cloud) need only a new `ArtifactKind`; **`IncidentCorrelationService`** folds every malicious detection into the best-matching open incident (updating its campaign) or opens a new incident + campaign; **relationship intelligence** ("this URL appears in N incidents", "this sender belongs to campaign X"); incident analyst workflow (status/priority/assignee/tags/comments) with an append-only investigation history that **never overwrites detection evidence**; `incidents` + `campaigns` tables + migration; `/api/incidents`, `/api/campaigns`, `/api/relationships`, workflow endpoint; live Incidents page (queue, campaigns, evidence, affected users, timeline, workflow). |
| M7·P1 | SOC Command Center | ✅ | **`SocOverviewService`** aggregating the entire platform from a **single repository snapshot** (one pass per repository, no per-widget queries) into security posture, incident metrics + queue, campaign intelligence, threat intelligence, a unified cross-source **timeline**, security analytics (risk distribution, 7-day detection trend, response/containment/false-positive rates), analyst activity, and platform health (reusing `HealthRegistry` plus ML/heuristic/threat-intel/config components); `/api/soc/overview` serving the whole dashboard in one request; **SOC Command Center** dashboard rebuilt as the operational hub with posture banner, sectioned widgets and **drill-down navigation** into Incidents and Threat Intelligence (navigation injected through `UIContext`). Fixes: posture never under-reports the worst open incident; stored (naive) timestamps normalized to UTC at the aggregation boundary; a malicious verdict can no longer carry category `none`. |
| M7·P1b | SOC Command Center — UI/UX refinement | ✅ | **Product polish only; backend and API unchanged.** New reusable presentation components: `MetricTile` (icon, severity accent, trend indicator, description), `HealthTile`, clickable `IncidentCard`/`CampaignCard` with severity stripes and quick actions, vertical `TimelineView` with icon markers and connecting rail, and `StatusPanel`/`SkeletonPanel` for professional empty, loading and recovery states. Dashboard reordered to SOC workflow priority (Executive Overview → Critical Incidents → Campaigns → Timeline → Threat Intelligence → Analytics → Platform Health → Analyst Activity); incident/campaign cards drill through to investigations; raw connection errors replaced by a recovery panel that reconnects automatically. |
| M7·P1c | SOC Command Center — production polish (UI frozen) | ✅ | **Product quality only; no backend, API or schema change.** Executive status bar of compact badges (threat level, incidents, campaigns, platform, backend, last update, auto-refresh readiness); **Operations quick-actions** panel of shortcuts; incident cards gain owner initial avatars, whole-card click and Enter/Space activation; campaign cards gain growth, status and incident count; timeline gains relative timestamps, Today/Yesterday grouping, artifact badges and expandable entries; health cards report Version / Latency / Mode / Last check, showing **"Not reported"** rather than inventing data; hover, focus and tooltip affordances throughout; all spacing, radii and card sizing standardized on the shared design tokens. |
| M8·P1 | File Intelligence Engine (full vertical slice) | ✅ | **Complete artifact-first File Intelligence vertical** through every layer. **Core:** platform IOC engine (`core/domain/ioc.py` — the single reusable extraction component for URL/Email/File/Reports, with refanging + URL/domain/IPv4/email/hash extraction); file domain (`core/domain/file.py`) with an **extensible fingerprint registry** (`FingerprintProvider` port + ordered registry; SHA-256/SHA-1/MD5 now, SSDEEP/TLSH/IMPHASH later with zero domain change), magic-signature type ID, MIME detection, Shannon entropy, double-extension and path-traversal safety; `FileScan`/`FileInvestigation` aggregates; **artifact-first** analyzer ports (`AnalyzedArtifact`, `IArtifactEvidenceProvider`, `IFileAnalyzer`, `IArchiveInspector`) so future artifact types (memory dumps, PCAPs, registry) reuse the same contracts. **AI:** `HybridFileAnalyzer` composing five offline evidence providers (structure, entropy, metadata, script/macro, indicators) through the shared `combine_evidence` policy. **Services:** `FileIngestor` (byte-free artifact, 25 MB cap), `FileAnalysisService` reusing the URL engine per embedded URL, recording malicious files to Threat Intelligence, and correlating into incidents/campaigns via a refactored shared `IncidentCorrelationService._correlate` + new `correlate_file`; `FileInvestigationService`. **Infrastructure:** `file_scans`/`file_investigations` tables (fingerprints as JSON for extensibility; **no file bytes ever persisted**), mappers, repositories, one Alembic migration. **API:** `POST /api/files/scan` (multipart, static-only, never executed), recent + investigation endpoints; SOC snapshot extended in the same single pass (Malicious files metric, analytics/trend/risk fold-in). **UI:** live File Scanner investigation workspace (overview, IOCs, embedded URLs, threat-intel/correlation, explainable evidence, analyst notes). Security: static-only, resource-bounded, path-traversal-safe, bytes discarded immediately. 46 new tests (300 total). |
| M8·P2a | Advanced File Intelligence — Deep Inspectors + IOC Platform | ✅ | **Four deep analysis providers** (`OfficeDocumentProvider`, `ArchiveProvider`, `ExecutableProvider`, expanded `ScriptProvider`) + **expanded platform IOC engine** (IPv6, JWT, AWS keys, API keys, Discord webhooks, Bitcoin wallets, stable `ioc_id` for Threat Graph, `IOCStatistics`, `IOCExtractionResult`). **Office:** VBA macros, AutoOpen/AutoExec, DDE fields, external template injection, embedded OLE, OOXML relationship inspection (exe/dll/script/PS1 references) via in-memory `zipfile`. **Archive:** dangerous embedded types, nested archives, filename masquerading, zip-bomb ratio/size heuristics, password detection, path traversal — all without disk extraction. **Executable:** static PE parser (`struct`-only, parsing separated from detection per approved architecture → `PEParser` → `PEInfo` → `ExecutableProvider`), sections, imports, exports, compile timestamp, packer heuristics, signature presence, VersionInfo anomalies. **Script:** obfuscation (chr/fromCharCode/atob/concat/env-var expansion), download cradles (Invoke-WebRequest/curl/wget/certutil/bitsadmin), dangerous PowerShell flags (-Enc/-NoProfile/-WindowStyle Hidden), ActiveX/WMI. **Explainability:** every provider returns provider name/version/execution_ms, analyst recommendation per contribution, optional MITRE ATT&CK technique_id (T1036/T1059/T1204/T1221/T1559/T1027/T1055/T1552/T1070). Zero new dependencies. Zero regressions (300 prior tests + 29 new = 329 total). |
| M8·P2b | Intelligence Fusion Layer | ✅ | **Unified intelligence fusion** across URL, email, and file engines. **Domain:** `Severity` (score→named level), `ProviderInfo`/`ProviderSummary` (provider diagnostics), `IntelligenceRelationship` (Threat Graph edge preparation with stable `ioc_id`-based linking), `FusionResult` (enriched report wrapping `IntelligenceReport` with severity, duration, recommendations, MITRE techniques, IOC summary, provider summaries, relationships). **Services:** `EvidenceFusionService` (deduplicates evidence, delegates to `combine_evidence`, computes severity, collects recommendations/techniques/provider summaries, tracks analysis duration); `ProviderRegistry` (runtime metadata for all providers — name/version/enabled/artifact types/config — supporting VirusTotal/YARA/Sandbox/Sigma without contract change); `IOCFusionService` (cross-artifact merge, `extract_relationships` artifact→IOC edges, `cross_correlate` finds shared IOCs between artifacts). **SOC:** Malicious hashes + Recent uploads metrics in the existing snapshot (same single pass). **Provider registration:** 10 providers registered at container build (8 file + URL heuristic + email hybrid). Zero new dependencies. Zero regressions (329 prior + 16 new = 345 total). |
| M8·P2c | Unified Investigation Workspace | ✅ | **Artifact-agnostic investigation workspace** establishing the platform standard for all current and future artifact types. **Domain:** `InvestigationSummary` (unified model: artifact type/ID, status, severity, confidence, timeline, evidence tree, metadata, recommendations, techniques, relationships, provider diagnostics, performance), `InvestigationEvent` (chronological timeline), `EvidenceNode` (hierarchical tree: provider → evidence → contribution → recommendation → MITRE), `MetadataField` (adaptive key-value metadata), `EventKind` enum. **Services:** `build_file_investigation` builder (constructs unified summary from file scan — future artifact types add a new builder, workspace unchanged). **UI components:** 10 reusable investigation panels (`InvestigationHeader`, `TimelinePanel`, `EvidenceTreePanel`, `RelationshipPanel`, `IOCPanel`, `MetadataPanel`, `ThreatHistoryPanel`, `ProviderDiagnosticsPanel`, `RecommendationsPanel`, `PerformancePanel`) — all artifact-agnostic, driven by the `InvestigationSummary`. **File Scanner rewritten** on the unified workspace with the approved layout: Header → Timeline → Evidence Tree → Relationships → IOC → Indicators → Metadata → URLs → Threat History → Provider Diagnostics → Recommendations → Performance → Analyst Notes. Extension points prepared for memory/registry/PCAP/cloud/identity/container/mobile — no workspace redesign needed. Zero new dependencies, zero regressions (345 prior + 2 new = 347 total). |
| M9·P1 | Internal Intelligence Event Bus | ✅ | **In-process, synchronous event dispatcher** decoupling intelligence producers from downstream consumers. **Domain:** 14 typed `EventType` values (`ARTIFACT_ANALYZED`, `IOC_EXTRACTED`, `THREAT_MATCHED`, `THREAT_RECORDED`, `INCIDENT_CREATED/UPDATED`, `CAMPAIGN_CREATED/UPDATED`, `INVESTIGATION_COMPLETED`, `INTELLIGENCE_REPORT_GENERATED`, `PROVIDER_STARTED/COMPLETED/FAILED`, `RELATIONSHIP_DISCOVERED`); `IntelligenceEvent` frozen VO with event_id, event_type, timestamp, correlation_id, source, artifact_id, payload; 11 typed convenience constructors. **Port:** `IEventBus` (publish/subscribe/subscriber_count) + `EventHandler` type alias in Core interfaces — future async or broker implementations swap behind the same contract. **Implementation:** `InProcessEventBus` — ordered delivery, failure isolation (failing handler logged but doesn't block others), per-publish and per-handler timing, accumulated `metrics` (total_published/dispatched/failures/publish_ms/type_counts). `EventHistory` — in-memory ring buffer (configurable max, default 1000), attaches to bus for all event types, exposes recent/count/type_statistics/recent_by_type/summary. **Container:** bus + history wired, `event-bus` health component with subscriber count, 10+ subscribers registered via history.attach. **Threat Graph seam:** a future `GraphRelationshipBuilder` subscribes to `RELATIONSHIP_DISCOVERED` without changing any publisher. Zero new dependencies (stdlib + existing Loguru). Zero regressions (347 prior + 19 new = 366 total). |
| M9·P2 | Knowledge Graph Domain | ✅ | **Complete knowledge graph as a platform capability.** **Domain:** `GraphNode` (13 `NodeType` values: artifact/url/domain/file/email/hash/ioc/threat/incident/campaign/investigation/provider/ip_address), `GraphEdge` (13 `RelationshipType` values: contains/references/shares_ioc/related_to/downloaded_from/delivered_by/generated/observed_in/targets/associated_with/analyzed_by/correlated_to/member_of), `GraphPath`, `GraphSnapshot`. **Port:** `IGraphRepository` (Core-owned, storage-agnostic — add_node/add_edge/update_metadata/get/neighbors/edges_of/nodes_by_type/shortest_path/shared_iocs/subgraph/snapshot); replaceable with Neo4j/Neptune/JanusGraph/Cosmos DB. **Implementation:** `InMemoryGraphRepository` (dict-backed, BFS traversal, deterministic dedup by key, adjacency index). **Builder:** `GraphBuilder` subscribes to 9 event types on the bus; publishers remain unaware of the graph. Creates nodes (artifact/threat/incident/campaign/provider) and edges (observed_in/associated_with/analyzed_by/contains) automatically. **Query:** `GraphQueryService` (lookup/neighbors/edges_of/related_artifacts/shared_iocs/incident+campaign relationships/investigation_subgraph/shortest_path/reachable) + analytics stubs (centrality/components/communities/attack_paths/blast_radius). **Health:** `knowledge-graph` component reporting node/edge counts. Zero new dependencies, zero regressions (366 prior + 29 new = 395 total). |
| M9·P3.0 | Baseline Architecture Remediation | ✅ | **Scoped remediation restoring 7/7 Import Linter contracts** ahead of M9 Phase 3; no features, no behaviour change. (1) **Services → AI severed:** the pure static PE parser (`parse_pe`) relocated to `core.domain.pe` beside the other byte→VO parsers; new Core `IPeParser` port; `FileIngestor` depends on the port (core parser as default), the AI layer supplies the injected `StructPeParser` adapter; `parse_pe` re-exported from `ai.file_analysis` for backward compatibility (byte-identical parsing verified). (2) **UI → Services severed:** the unified `InvestigationSummary` is now built server-side and delivered to the presentation layer as a DTO on the file-scan response (`application.api.file`), reconstructed via `BackendClient`; the `services.investigation` import removed from `ui.pages.file_scanner`. No schema change (migration head unchanged, no drift). Gates: Black · Ruff · Mypy (strict) · Import Linter **7/7** · Pytest · Alembic no-drift — all green. Zero regressions (395 prior + 2 new = 397 collected; 387 test functions). Frozen as the new architectural baseline. See `reports/M9P3.0_Validation_Report.md`. |
| M9·P3-A | Graph Explorer Application Layer | ✅ | **Reusable backend capability between the Knowledge Graph and the future Explorer UI; no presentation implementation.** **Presentation contracts:** pure, framework-free graph view DTOs in `core/domain/graph_view.py` (`GraphNodeView`, `GraphEdgeView`, `GraphView`, `GraphSelection`, `GraphPathView`, `GraphSnapshotView`, `GraphAnalyticsSummary`, `ConnectedEntity`, `GraphSearchResult`) — shared by producer and UI, same precedent as `InvestigationSummary`. **Service:** `GraphExplorerService` (`services/graph/explorer.py`) orchestrates the frozen `GraphQueryService` (traversal/query) and reuses `IGraphRepository` (node enumeration) — reimplements no traversal; maps domain→view DTOs, derives display tone/risk, bounds views (cap 250 + `truncated`). Operations: snapshot, node lookup, neighbourhood expansion, shortest path, shared-IOC discovery, investigation/incident/campaign subgraphs, search (id/label/metadata, auto-focus), selection, lightweight analytics (most-connected, IOC count, largest component, reachability). **API:** `/api/graph/*` (snapshot, analytics, search, path, shared-iocs, investigation, incident, campaign, node, neighbors, selection) returning presentation DTOs only — 404 on missing node, 422 on empty search. **DI:** explorer built at the composition root and injected into `create_api`; router registered. **Gateway:** `BackendClient` graph methods returning reconstructed view DTOs with safe defaults on error; DTOs re-exported from `ui.backend`. Layering `UI → BackendClient → REST API → GraphExplorerService → GraphQueryService → IGraphRepository` enforced by Import Linter. Zero new dependencies. Gates: Black · Ruff · Mypy (strict, 306 files) · Import Linter **7/7** · Pytest · Alembic no-drift — all green. Zero regressions (397 prior + 37 new = 434 collected). STOP for architectural review before P3-B. See `reports/M9P3-A_Validation_Report.md`. |
| M9·P3-B | Intelligence Graph Explorer (MVVM Presentation Layer) | ✅ | **Analyst-facing Graph Explorer built entirely on the frozen P3-A backend over HTTP; presentation layer only, no backend behaviour change.** **Layout:** pure, deterministic Fruchterman-Reingold spring layout (`ui/components/graph/layout.py`), Qt-free and unit-tested. **Identity:** distinct categorical colour + glyph + label per node type (`identity.py`). **Scene items:** `GraphNodeItem` (draggable/selectable/hover, glyph+label) and `GraphEdgeItem` (endpoint-tracking, highlight/dim). **Canvas** (`canvas.py`, QGraphicsView): pan, wheel-zoom, node drag, fit-to-view, focus, hover relationship highlighting, visibility filtering; emits selection/expansion intents + render/layout timing. **Toolbar:** fit, zoom, reload, expansion-depth. **Panels** (`panels.py`): Search (+history, match highlighting), Filters (node type/relationship/confidence), Timeline (edge-timestamp cutoff), Node Details (+ expand/focus/open-investigation pivots), Relationship Details, Analytics (+ observability: layout/render/expand ms), Legend. **View-model** (`ui/viewmodels/graph_explorer.py`): owns loaded graph, filters, cutoff, selection; backend access off the UI thread; client-side filtering/timeline; expand merges neighbourhoods in place; **injectable `runner_factory` (Dependency Inversion) for deterministic tests**. **Page** (`ui/pages/graph_explorer.py`): thin MVVM view composing toolbar+canvas+panels. **Navigation:** new `GRAPH_EXPLORER` route + sidebar entry; **backward-compatible payload-aware navigation** (`Router.navigate`/`UIContext.go_to` optional `payload` → page `on_navigated` hook). **Investigation integration:** "Open in Graph Explorer" from the File Investigation workspace focuses the artifact; explorer offers "Back to investigation" — no investigation logic duplicated. Layering `GraphExplorerPage → GraphExplorerViewModel → BackendClient → (HTTP) /api/graph/*` enforced by Import Linter (UI→Core-only holds). Zero new dependencies, no schema change (migration head unchanged, no drift). Gates: Black · Ruff · Mypy (strict, 321 files) · Import Linter **7/7** · Pytest · Alembic no-drift — all green. Zero regressions (434 prior + 35 new = 469; non-UI 353 + UI 116). **Known follow-up (backend, out of scope):** detection services do not yet publish intelligence events, so the live graph is populated only in tests. STOP for architectural review before P3-C. See `reports/M9P3-B_Validation_Report.md`. |
| M9·P3-C | Intelligence Graph Explorer — Hardening, Observability & Release | ✅ | **Finalisation of the Graph Explorer; no new architectural subsystems, no persistence, no event-publisher wiring.** **Lightweight analytics:** real BFS-based `centrality`, `connected_components`, `graph_density`, `blast_radius`, and shortest-path `attack_paths` on `GraphQueryService` (`communities` explicitly out of scope); `GraphAnalyticsSummary` extended with `relationship_type_counts`, `component_count`, `density` (additive) and surfaced through API, `BackendClient`, and the Analytics panel (entity + relationship distributions, component/density/blast-radius structure). Explorer aggregation reuses `connected_components` (DRY). **Observability:** view-model `metrics_ready` signal carrying backend-query, layout, render, expansion, search, and timeline-filter durations plus node/edge/visible/hidden/expansion/depth counts, rendered in the Analytics panel; backend `GraphExplorerService.metrics()` (via a `@_tracked` leaf decorator that avoids double-counting delegations) surfaced through the existing health system as a new `graph-explorer` component. **Session preparation (in-memory only):** `ExplorerSessionState`/`ViewportState` with page-level `session_state()`/`restore_session()` and canvas viewport capture/restore — no persistence. **Polish:** canvas keyboard navigation (+/− zoom, F/Home fit, arrow-key pan). **Documentation (deferred batch completed):** Graph Explorer architecture, backend boundary, knowledge graph, event-bus interaction, ADR-0001, Graph API reference, developer guide, user guide, `CHANGELOG.md`, `ROADMAP.md`. Gates: Black · Ruff · Mypy (strict, 323 files) · Import Linter **7/7** · Pytest · Alembic no-drift — all green. Zero regressions (469 prior + 11 new = 480; non-UI 357 + UI 123). **Deferred to M10:** graph + session persistence behind unchanged ports; live-graph population via detection-service publishers. STOP for architectural review before M10. See `reports/M9P3-C_Validation_Report.md`. |
| M10 | Live Intelligence Pipeline | ✅ | **Event-driven live graph population; no persistence, no database technology, no repository-interface change, no duplicated publisher logic.** Introduced a single publishing seam `services/pipeline/publisher.py::IntelligencePublisher` — all intelligence-event construction is centralised there; each detection service extracts its facts and calls one method. Best-effort publishing (failures counted/logged, never break analysis). **URL/email/file analysis** publish `artifact_analyzed` (+ `ioc_extracted`, per-IOC/embedded-URL `relationship_discovered`, `threat_recorded` when malicious); **email/file** also publish `incident_created`/`campaign_created` on existing correlation; **investigation services** publish `investigation_completed` on save (new constructor added; `INVESTIGATION_COMPLETED` type already existed). The already-subscribed **GraphBuilder required no change** — publishers emit only event types it consumes — so the Knowledge Graph now populates live and the Graph Explorer reflects newly analysed intelligence. Container restructured to build the bus/graph/publisher before the detection services and inject the publisher (optional, default `None` → full backward compatibility). **Observability:** enriched `event-bus` health (published/dispatched/failures) + new `graph-builder` (build count + processing time) and `intelligence-publisher` (events published + failures) health components; per-type publisher metrics. Gates: Black · Ruff · Mypy (strict, 327 files) · Import Linter **7/7** (intra-services `services.pipeline` import is inward/sideways) · Pytest · Alembic no-drift — all green. Zero regressions (480 prior + 14 new = 494; non-UI 371 + UI 123); no schema change. Docs: `docs/architecture/Live_Intelligence_Pipeline.md` + Event Bus/Knowledge Graph/Graph Explorer/Backend Boundary/API/User docs updated to remove the deferred-publishing caveat. STOP for architectural review before M11. See `reports/M10_Validation_Report.md`. |
| M11 | Advanced Threat Analytics & Intelligence Engine | ✅ | **Intelligence-driven analytics engine composed entirely over the existing platform; no redesign, no persistence, no database, no repository change, no duplicated algorithms.** All analytics deterministic and explainable (every scoring/analysis DTO carries a `rationale`). **Phase A — Graph Analytics:** `GraphAnalyticsService` (degree, centrality ranking, connected components, density, blast radius, reachability, shortest attack paths, multi-hop neighbourhood, shared-infrastructure discovery via origin→infra→peer, threat propagation) composing `GraphQueryService`. **Phase B — Threat Intelligence:** `IOCIntelligenceService` (frequency/prevalence/reuse/confidence/aging), `CampaignIntelligenceService` (size/evolution/Jaccard similarity), `ThreatScoringService` (severity/exposure/confidence/priority/analyst-urgency, reusing Phase A blast radius). **Phase C — Attack Analysis:** `AttackAnalysisService` (attack-chain reconstruction, kill-chain mapping, compromise-path discovery, root-cause analysis, infrastructure clusters, attack timeline, threat propagation). **Phase D — Recommendations:** `RecommendationService` (next investigation, priority IOC, risk campaign, suspicious relationship, containment order, investigation sequence — each explained; reuses B/C scores, no duplicated logic). **Phase E — SOC & Explorer:** `AnalyticsOverviewService` + `GraphOverlayService`; additive `/api/analytics/{overview,overlay}` endpoints + `BackendClient` gateway; additive SOC dashboard "Advanced Threat Analytics" section (second async fetch, frozen flow untouched); Explorer "Analytics Overlay" toggle reusing the canvas `highlight_nodes`. **Shared:** `MeteredService` + `tracked` decorator (`services/analytics/observability.py`) for uniform `metrics()` with no duplicated timing; reusable `GraphQueryService.all_nodes()` primitive; container `_build_analytics_engine()` with `graph-analytics` + aggregate `intelligence-engine` health components. DTOs: `core/domain/{analytics,intelligence,attack,recommendation,soc_analytics}_view.py` (frozen, framework-free). Gates: Black · Ruff · Mypy (strict, 347 files) · Import Linter **7/7** · Pytest · Alembic no-drift — all green. Zero regressions (494 prior + 33 new = 527; non-UI 402 + UI 125); no schema change. Docs: `docs/architecture/Advanced_Threat_Analytics.md`. STOP for architectural review before M12. See `reports/M11_Validation_Report.md`. |
| M12·P1 | AI Security Copilot — Reasoning Engine | ✅ | **Read-only AI Security Copilot composed entirely over the deterministic platform; no redesign, no persistence, no database, no repository change, no duplicated intelligence logic. The Copilot is never a source of truth (ADR-0002).** **Pipeline (`services/copilot/**`):** `CopilotOrchestrator` sequences deterministic intent detection → skill selection → read-only context collection → prompt building → provider inference → citation validation → grounding validation → response formatting; each stage independently testable. **Five skills** (Threat Investigation, IOC Intelligence, Graph Reasoning, Incident Analysis, Executive Summary) via `SkillRegistry` + `ICopilotSkill` — extensible by registration only. **`ContextCollector`** reads only the existing M11 analytics/graph/attack/recommendation services and renders their DTOs to ranked `ContextItem`s (severity-ranked, token-budget-bounded, graph-driven discovery); no scoring/traversal/correlation/recommendation logic reimplemented. **Grounding:** system prompt mandates context-only answers + `[cite:KIND:ID]` citations + explicit insufficiency; `CitationValidator` resolves markers against provided context; `GroundingValidator` scores coverage (strict mode refuses ungrounded answers). **Provider-agnostic:** Core `ILLMProvider` port; `ClaudeProvider` (Anthropic Messages API over `httpx`) built on `BaseLLMProvider`; config-driven factory; streaming seam reserved (Phase 2); graceful "unavailable" degradation on any provider failure. **Sessions:** in-memory `SessionManager` (LRU-bounded, per-session turn cap) + analyst `FocusState`; no persistence. **API:** additive `POST /api/copilot/ask`, `GET`/`DELETE /api/copilot/session/{id}`, `POST /api/copilot/session/{id}/focus` + `BackendClient` gateway. **Wiring:** container `_build_copilot()`; `ai-copilot` health component (optional — never degrades platform status). **Config:** new `copilot` section (`config/copilot.yaml`, env overrides `AEGIS_COPILOT_{ENABLED,PROVIDER,MODEL}`); API key via `copilot.api_key_env` (no secret in config). DTOs/ports: `core/domain/copilot{,_session}.py`, `core/interfaces/{llm_provider,copilot_skill}.py` (frozen, framework-free). Gates: Black · Ruff · Mypy (strict, 365 files) · Import Linter **7/7** · Pytest · Alembic no-drift — all green. Zero regressions (527 prior + 45 new = 572; non-UI 447 + UI 125); no schema change. Docs: `docs/architecture/AI_Security_Copilot.md`, `docs/architecture/adr/ADR-0002-copilot-not-source-of-truth.md`. STOP for architectural review before M12 Phase 2 (UI + streaming). See `reports/M12P1_Validation_Report.md`. |
| M12·P2 | AI Security Copilot — User Experience | ✅ | **MVVM presentation layer over the Phase 1 Copilot backend; UI-only, no new intelligence logic, no new detection, no new graph algorithms, no direct service access. Communicates exclusively through `BackendClient` and the existing REST API.** **View-model** (`ui/viewmodels/copilot.py`): `CopilotViewModel` + `ChatTurn` — owns the active-session conversation, runs `copilot_ask`/`update_focus`/`close_session` off the UI thread via an injected `AsyncRunner`, exposes turn/busy/error/cleared/focus signals, maps provider failures to analyst-friendly messages. **Page** (`ui/pages/copilot.py`): auto-scrolling transcript, composer (Enter=send, Shift+Enter=newline), context-aware suggested prompts, clear/regenerate/copy, developer-mode metadata (skill, duration, provider, grounding; model + prompt version in dev mode), citation click-through, and an `on_navigated({focus, kind, origin, prompt})` launch hook. **Components** (`ui/components/copilot/chat.py`): `MessageBubble`, `CitationChip`, `CitationsRow`, `MetadataRow`, `TypingIndicator`, `SuggestedPrompts`, `ChatComposer`. **Citations:** `citation_target` maps a `Citation` to the Graph Explorer via the existing `{focus, origin}` contract — clickable chips navigate to the referenced node. **Launch points:** additive "Ask Copilot" action on URL/email/file investigations, incident triage, Graph Explorer (selected node), and SOC dashboard (global), each passing `{focus, kind, origin}` so the *backend* collects context (no UI-side context logic). **Shell:** `Route.COPILOT` + "AI Copilot" sidebar entry; `copilot` icon; Copilot QSS section. **Streaming:** reserved seam; standard path with graceful fallback. **Errors:** provider-unavailable/timeout/network/insufficient-context surfaced as calm messages — never a stack trace. **Accessibility:** Enter/Shift+Enter, focus indicators, scalable fonts, high-DPI, shared tokens. Gates: Black · Ruff · Mypy (strict, 373 files) · Import Linter **7/7** · Pytest · Alembic no-drift — all green. Zero regressions (572 prior + 38 new = 610; non-UI 447 + UI 163); no schema change, no API change, no config change. Docs: `docs/architecture/AI_Security_Copilot.md` (presentation-layer section added). STOP for architectural review before M12 Phase 3. See `reports/M12P2_Validation_Report.md`. |
| M12·P3 | AI Security Copilot — Finalization | ✅ | **Final Copilot phase: streaming, reliability, UX polish, and Qt lifecycle hardening over the Phase 1 backend and Phase 2 UI. No backend redesign, no new engine/graph/persistence/database, no new AI abstraction. The `ILLMProvider` contract is extended only additively.** **Streaming (provider):** `LLMStreamChunk` + optional `supports_streaming()`/`stream()` with safe defaults on `ILLMProvider`; `BaseLLMProvider.stream()` (SSE over httpx, failures → terminal chunks, never raises); `ClaudeProvider` advertises streaming + parses the Anthropic SSE sequence. **Streaming (orchestrator):** `_prepare`/`_finalize` refactor (ask unchanged); `stream_ask()` streams raw tokens but runs **citation + grounding validation on the complete text** and emits the validated `CopilotResponse` on the terminal `final` event — ADR-0002 preserved (ungrounded stream → honest \"insufficient\" answer); `CopilotStreamEvent` domain type. **Transport:** additive `POST /api/copilot/ask/stream` (SSE); `/ask` unchanged as fallback; `BackendClient.copilot_stream()` generator + graceful transport-error fallback. **UI:** lifecycle-safe `StreamWorker` (owned `QThread`, cooperative cancellation, post-cancel signal suppression, quit+join on `stop()`); view-model `token_received` signal, streaming dispatch with non-streaming fallback, injectable `stream_worker_factory`, `dispose()`; page progressive token rendering, generating state, scroll-during-generation, `closeEvent` + `aboutToQuit` disposal, injectable `view_model`. **Reliability verified (§4–§6):** fully/partially grounded, unsupported claim, insufficient context, nonexistent artifact, invalid/missing citation, provider available/unavailable/timeout/malformed/network/interrupted/empty — all analyst-friendly, no stack traces. **Qt lifecycle audit (§7):** QTimers, workers, AsyncRunner, streaming workers, signals, page destruction, app shutdown — **full UI suite exits cleanly (exit 0), no leaked workers**; fixed a real defect where pre-existing VM tests spawned real workers once streaming defaulted on. Gates: Black · Ruff · Mypy (strict, 378 files) · Import Linter **7/7** · Pytest · Alembic no-drift — all green. Zero regressions (610 prior + 27 new = 637; non-UI 459 + UI 178); no schema/persistence/config change. Docs: `docs/architecture/AI_Security_Copilot.md` (streaming section). **M12 COMPLETE.** STOP for architectural review; M13 not started. See `reports/M12P3_Validation_Report.md`. |

**M1b — Walking Skeleton: COMPLETE.** Knowledge Base synchronization from the Documentation Impact Log (D23–D31) is now due.

**M1a review & KB sync:** Architecture Review complete; consolidated KB
Synchronization package (D1–D22) generated under `docs/architecture/`.

---

## Blockers

_None._

---

## Upcoming

- **Deferred KB syncs (batched, per direction):** M1b (D23–D31) and M2 (D32–D37)
  consolidated Knowledge Base updates, to be generated at a later milestone.
  Traceability is preserved in the Documentation Impact Log and IMPLEMENTATION_LOG.
- **Next milestone:** M3 — a second detection vertical (Email or File analysis)
  reusing the URL vertical's pattern, and/or introducing the trained LightGBM URL
  model behind the existing `IUrlAnalyzer` port.
| M13 | Authentication & Secure Application Entry | ✅ | **Full production milestone: professional single-user local authentication. Not an enterprise identity system (no multi-user/RBAC/SSO/LDAP/cloud/multi-tenancy); exactly one local account.** **Decision (ADR-0003):** stdlib `hashlib.scrypt` (memory-hard, zero new dependency) chosen over bcrypt/argon2/passlib; per-account 16-byte salt; self-describing `scrypt$n$r$p$salt$hash`; constant-time verify, `False` on malformed. Server-side opaque bearer-token sessions (expiry, logout-invalidation) in SQLite; enforcement at the API boundary. **Core:** `domain/auth.py` (`User`, `AuthSession`, `AuthenticatedUser`, tz-safe expiry); ports `IPasswordHasher`, `IUserRepository`, `IAuthSessionRepository`; `security/auth_policy.py` (validation + normalization). **Infrastructure:** `ScryptPasswordHasher` (stdlib); `UserAccountRow`/`AuthSessionRow`; SQLAlchemy repositories (case-insensitive lookup, single-account check, conflict detection, expiry purge). **Migration:** `f163ed341c79` (`user_accounts`, `auth_sessions` + indexes); no drift, 9 migrations. **Services:** `AuthenticationService` (register/login/logout/current-user/purge) via a session-scoped unit of work; generic `INVALID_CREDENTIALS` + dummy-hash timing mitigation (no enumeration); single-account enforced. **API:** `/api/auth/status|register|login|me|logout` (validated DTOs, 201/409/422/401/204, hashes never returned); `require_session` guards **all** analyst routers (analysis, email, file, threats, incidents, soc, graph, analytics, copilot). **Client:** token storage, bearer attachment on all calls (34 call sites routed through auth-aware wrappers), 401→session-expired handler, `auth_status/register/login/logout/current_user`. **UI:** `AuthViewModel` (off-thread); premium `AuthWindow` (branded hero + stacked login/register/loading/success cards, all 13 states, inline validation, password visibility, strength meter, Tab order, Enter-submit, session-expired notice); `auth_fields` components; 5 new icons; auth QSS; **visual polish pass** (fixed input/error overlap, stuck button, spacing). **Flow:** `DesktopAuthFlow` — auth shown before shell; on login builds+shows `MainWindow`; **logout** action in top bar returns to auth; 401 routes back with expiry notice; shell never built until authenticated. **Qt shutdown fix:** `BackendHealthPoller` active-guard + drain so no worker signal fires into a torn-down window (clean exit). Gates: Black · Ruff · Mypy (strict, 396 files) · Import Linter **7/7** · Pytest · Alembic no-drift — all green. Zero regressions (637 prior + 41 new = 678; non-UI 479 + UI 199, clean UI exit). Real launch verified: first-launch register → login → SOC → navigate → logout → subsequent-launch login. Docs: ADR-0003, `docs/architecture/Authentication.md`, 3 M13 reports. **Authentication experience considered COMPLETE.** STOP for architectural review; Gmail/M14/candidate-release not started. See `reports/M13_Validation_Report.md`. |
| M14 | Gmail Intelligence Integration | ✅ | **Read-only Gmail INPUT CONNECTOR feeding the EXISTING Email Analysis pipeline — not a new engine.** No GmailThreatAnalyzer / Gmail-specific IOC / scoring / graph / Copilot skill. **Decision (ADR-0004):** httpx (no google-api-python-client/google-auth) + installed-app **loopback OAuth 2.0** (system browser, ephemeral `127.0.0.1` callback, `state` nonce, lifecycle-safe listener torn down on success/denial/timeout/error/exit; no copy-paste, no webview). **Scope strictly `gmail.readonly`**, enforced by a config validator at load. **Core:** `domain/gmail.py` (`GmailCredentials/Account/MessageRef/RawMessage`); ports `IGmailAuthFlow`, `IGmailTokenStore`, `IGmailGateway`, `IGmailSyncStateRepository`. **Config:** `GmailSettings` + `gmail.yaml` (client id via `AEGIS_GMAIL_CLIENT_ID`/yaml; secret via `AEGIS_GMAIL_CLIENT_SECRET` env). **Infrastructure (httpx/stdlib, no Google SDK):** `LoopbackGmailAuthFlow`, `HttpxGmailGateway` (profile/list/fetch-raw `format=raw`, 401→auth-error), `FileGmailTokenStore` (0600 JSON under `data/gmail/`, outside repo). **Dedup:** `GmailProcessedMessageRow` + repository; migration `2e323b64d4cd` (additive; no drift; 10 migrations); `EmailScan` untouched. **Service:** `GmailIngestionService` — refresh-on-expiry, dedup by Gmail message id, feeds raw RFC-822 into **existing `EmailAnalysisService.analyze()`**; stats (retrieved/analyzed/duplicates/malicious/suspicious/benign/errors/last-sync). **API:** `/api/gmail/status|connect|disconnect|sync` behind M13 `require_session` (separate from Gmail OAuth identity); no token/secret in any response; graceful errors (no stack traces). **UI:** `GmailViewModel` (off-thread) + polished `GmailPage` (disconnected/connecting/connected/syncing/sync-result states; Read-only badge; Sync Now/Disconnect; monitoring OFF) in a new Integrations sidebar section; BackendClient gmail methods. **Security:** `.gitignore` covers `data/gmail/`, `*token*.json`, credential files. Gates: Black · Ruff · Mypy (strict, 414 files) · Import Linter **7/7** (Google code isolated; email/graph/Copilot never import Gmail types) · Pytest · Alembic no-drift — all green. Zero regressions (678 prior + 42 new = 720; non-UI 513 + UI 207, clean UI exit). E2E proven: fake Gmail → GmailIngestionService → **real** EmailAnalysisService → real graph nodes → SOC; dedup across syncs. **Live Google OAuth = documented manual step** (Google domains not on this env's allowlist; not faked). Docs: ADR-0004, `docs/architecture/Gmail-Integration.md`, `Gmail-Security.md`, 3 M14 reports. STOP for architectural review; **M15 Candidate Release not started.** See `reports/M14_Validation_Report.md`. | **COMPLETION PASS (analyst workspace):** added the Gmail message workspace (list with existing verdict/risk, filters All/High/Suspicious/Benign/Unanalyzed, search), message detail surfacing the EXISTING analysis (verdict/risk/confidence/evidence/sources/IOCs/incident+campaign) + a SAFE plain-text preview (links marked untrusted, never auto-opened; attachments metadata only), and REUSE-ONLY navigation to the existing Email Investigation (`scan_id`), Graph Explorer (email-artifact focus), and Copilot (incident/artifact focus). Read-model: additive migration `32799204f010` expands `gmail_processed_messages` to a composite PK `(account_email, message_id)` + non-secret metadata (sender/subject/received/snippet/status) making dedup and the workspace ACCOUNT-AWARE (multi-account demo isolation; disconnect/reconnect clean). Four-state taxonomy ANALYZED/UNSUPPORTED/TRANSIENT/FAILED — the observed "1 error" is now explained as an UNSUPPORTED (unparseable) message, recorded and shown without a stack trace, and never fails the sync. New API `GET /api/gmail/messages`, `/messages/{id}`, `GET /api/email/scans/{id}` (session-guarded, DTOs only). Additive `EmailAnalysisService.get_scan` reader; no existing contract modified. Gates all green (Mypy strict 416 files · Import Linter 7/7 · 741 tests = 528 non-UI + 213 UI · Alembic no-drift, 11 migrations). +21 tests, zero regressions. Headless MainWindow smoke: Gmail workspace + all deep-links navigate, clean shutdown. Live Gmail steps remain a documented manual validation. M15 NOT started. |
